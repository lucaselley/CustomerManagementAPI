import { ActionTranslationPipe } from './action-translation.pipe';

describe('ActionTranslationPipe', () => {
  it('create an instance', () => {
    const pipe = new ActionTranslationPipe();
    expect(pipe).toBeTruthy();
  });
});
